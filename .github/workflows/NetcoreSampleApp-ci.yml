name: 'NetcoreSampleApp CI Pipeline'

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  DYNATRACE_ENVIRONMENT_ID: ${{ secrets.DYNATRACE_ENVIRONMENT_ID }}
  DYNATRACE_API_TOKEN: ${{ secrets.DYNATRACE_API_TOKEN }}
  SPLUNK_HEC_URL: ${{ secrets.SPLUNK_HEC_URL }}
  SPLUNK_HEC_TOKEN: ${{ secrets.SPLUNK_HEC_TOKEN }}
  NEXUS_REPO_URL: ${{ secrets.NEXUS_REPO_URL }}
  NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
  NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
  REGISTRY_URL: ${{ secrets.REGISTRY_URL }}

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: 'Set up .NET'
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: 'Restore & build'
        run: |
          dotnet restore "src/SampleApi/SampleApi.csproj"
          dotnet restore "tests/SampleApi.Tests/SampleApi.Tests.csproj"
          dotnet build "src/SampleApi/SampleApi.csproj" --configuration Release --no-restore
          dotnet build "tests/SampleApi.Tests/SampleApi.Tests.csproj" --configuration Release --no-restore
      - name: 'Run unit tests'
        run: |
          cat > coverlet.runsettings <<'EOF'
          <RunSettings><DataCollectionRunSettings><DataCollectors><DataCollector friendlyName="XPlat Code Coverage"><Configuration><Format>opencover</Format></Configuration></DataCollector></DataCollectors></DataCollectionRunSettings></RunSettings>
          EOF
          dotnet test "tests/SampleApi.Tests/SampleApi.Tests.csproj" --configuration Release --verbosity normal --collect:"XPlat Code Coverage" --settings coverlet.runsettings --results-directory ./TestResults
      - name: 'Sonar: set up JDK'
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: 'Sonar: install scanner'
        run: |
          dotnet tool install --global dotnet-sonarscanner
      - name: 'Sonar scan begin'
        run: |
          dotnet-sonarscanner begin /k:"Komali612_netcore-sample-app" /o:"komali612" /d:sonar.host.url="${SONAR_HOST_URL:-https://sonarcloud.io}" /d:sonar.token="$SONAR_TOKEN" /d:sonar.qualitygate.wait=false /d:sonar.exclusions="**/.github/**,**/Program.cs" /d:sonar.cs.opencover.reportsPaths="**/coverage.opencover.xml"
      - name: 'Sonar build'
        run: |
          dotnet build "src/SampleApi/SampleApi.csproj" --configuration Release
          dotnet build "tests/SampleApi.Tests/SampleApi.Tests.csproj" --configuration Release
      - name: 'Sonar scan end'
        run: |
          dotnet-sonarscanner end /d:sonar.token="$SONAR_TOKEN"
      # SAST - not selected in the orchestrator UI
      # - name: 'Run SAST (not selected)'
        # run: |
          # echo "SAST analysis via not selected (placeholder)"
      # SCA - not selected in the orchestrator UI
      # - name: 'Run SCA (not selected)'
        # run: |
          # echo "SCA analysis via not selected (placeholder)"
      # DAST - not selected in the orchestrator UI
      # - name: 'Run DAST (not selected)'
        # run: |
          # echo "DAST analysis via not selected (placeholder)"
      # IMGSCAN - not selected in the orchestrator UI
      # - name: 'Scan container image (not selected)'
        # run: |
          # echo "Container image scan via not selected (placeholder)"
      # ARTIFACT - not selected in the orchestrator UI
      # - name: 'Publish artifacts (not selected)'
        # run: |
          # echo "Publish build artifacts to not selected (placeholder)"
      - name: 'Log in to registry (GHCR)'
        uses: docker/login-action@v3
        with:
          registry: 'ghcr.io'
          username: '${{ github.actor }}'
          password: '${{ secrets.GITHUB_TOKEN }}'
      - name: 'Build and push image'
        run: |
          REPO=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          IMAGE=ghcr.io/$REPO:${{ github.sha }}
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
      - name: 'Generate SBOM'
        run: |
          echo "SBOM generation (placeholder - e.g. Trivy/Syft)"
      # MONITORING - not selected in the orchestrator UI
      # - name: 'Send telemetry (not selected)'
        # run: |
          # echo "Send build metrics/logs to not selected (placeholder)"
